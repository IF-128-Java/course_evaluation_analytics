CREATE TABLE COURSE_FEEDBACK_REQUEST
(
    ID int NOT NULL PRIMARY KEY,
    COURSE_ID int,
    FEEDBACK_DESCRIPTION varchar(255),
    START_DATE TIMESTAMP DEFAULT NOT NULL,
    END_DATE  TIMESTAMP DEFAULT NOT NULL
);